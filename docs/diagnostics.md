# Диагностика: стек и аппаратный отказ

## Диагностика: замер стека

`Board::init` первой строкой зовёт `stack::paint()` — заливает свободный стек
узором, чтобы `bsp::stack::never_used()` потом сказал, сколько байт ни разу не
понадобилось. Три вещи здесь нельзя трогать не разобравшись:

- **Символ называется `_hw_ram_start`, а не `_ram_start`.** Второе имя уже
  занято `cortex-m-rt` (`PROVIDE(_ram_start = ORIGIN(RAM))` в его `link.x`), и
  смысл у него ровно противоположный. Пропади наше определение — линкер молча
  подставил бы чужое, замер вернул бы ноль, и никто бы не заметил.
- **В `memory.x` это литерал, а не `ORIGIN(RAM)`.** `flip-link` переопределяет
  блок `MEMORY`, сдвигая начало RAM вверх на размер статики, так что после него
  `ORIGIN(RAM)` — вершина стека. Проверено по ELF: символ совпадал с
  `_stack_start`, и замер всегда давал ноль.
- **Заливка идёт до `init_peripherals`, но после установки указателя стека.**
  Раньше нельзя (заливка работает на том же стеке), позже — теряется расход
  инициализации. Без `flip-link` она затёрла бы статику целиком, поэтому в
  debug стоит `debug_assert!`.

## Диагностика: дамп аппаратного отказа

`#[panic_handler]` ловит только `panic!`. Разыменование мусорного указателя,
невыровненный доступ, переполнение стека, прыжок по битому адресу приходят
вектором `HardFault`, и до паникёра дело не доходит вовсе — `cortex-m-rt`
подставляет туда бесконечный цикл, то есть плата замирает молча. Закрывает это
`#[exception] fn HardFault` в `crates-cross/app/src/main.rs` поверх
`bsp::fault`. Что здесь нельзя нарушать:

- **Формат записи воспроизведён из `panic-persist`, а не взят у него.**
  Публичный API записи там один — `report_panic_info(&PanicInfo)`, а
  `PanicInfo` для аппаратного отказа не существует (writer `struct Ram`
  приватный). Поэтому раскладка повторена в `bsp::fault`: магия по смещению 0,
  длина по 4, текст с 8. Взамен на стороне чтения не меняется ничего —
  `Board::last_panic`, печать при старте и `cargo xtask panic` работают как
  работали. Скрытую связь стережёт target-тест
  `a_hard_fault_dump_reads_back_as_a_panic_message`: пишет нашим writer'ом,
  читает `panic-persist`, сравнивает. Магия при этом лежит в трёх местах
  (`bsp::fault`, `panic-persist`, `PANIC_MAGIC` в `xtask`) — цена того, что
  крейт не отдаёт writer наружу.
- **Уже стоящую запись обработчик не затирает.** Под debug паникёр
  заканчивается `udf()`, то есть тем же `HardFault`, — без этого правила
  настоящая причина паники подменялась бы на каждом падении бесполезным
  «отказ по адресу udf». Проверяется тестом
  `a_second_fault_keeps_the_first_reason`.
- **То же правило стоит и в паникёре — `if !bsp::fault::occupied()`.**
  `panic_persist::report_panic_info` своей проверки не делает и пишет поверх
  чего угодно, а прийти в паникёр можно уже с записанным дампом отказа:
  `defmt-rtt` панкует «taken reentrantly», если логгер занят, а обработчик
  отказа печатает свою строку, не зная, не упали ли мы посреди чужой печати.
  Снимете проверку — устройство в поле сообщит жалобу логгера вместо
  регистров отказа.
- **`fault::park()` и `fault::trap()` — разные вещи, не сливать.** `trap()`
  (хвост паникёра под debug) снимает `SHCSR.USGFAULTENA` и делает `udf()`:
  при включённом `UsageFault` неизвестная инструкция подняла бы именно его, и
  пробник, стерегущий вектор `HardFault`, остановки бы не увидел. `park()`
  (хвост обработчика отказа под debug) — обычная петля на `wfi`: `udf()`
  изнутри `HardFault` это отказ во время обработки отказа, то есть Lockup.
- **Ветка Armv6-M выбирается Liquid-условием (`armv6m`), а не Cargo-фичей** —
  по общему правилу шаблона (см. «Feature-flag конвенции»). У Cortex-M0/M0+
  регистров разбора отказа нет вовсе: `CFSR`, `HFSR`, `MMFAR`, `BFAR`, `SHCSR`
  появились в Armv7-M, в `cortex-m` закрыты `#[cfg(not(armv6m))]`, и код с
  ними на таком чипе не собирается. Признак считает `chip-select.rhai` по
  итоговому `target` (не по `chip_feature`: `--define target=...` разрешён).
  Обе ветки проверяются `template-check` — `stm32f407ve` и `stm32l011f4`.
- **Строка дампа без префиксов `0x` и без расшифровки битов.** Регион `PANIC`
  бывает всего 128 байт (чип с 2 KiB RAM, `RESERVED_MIN` в `chip-data-gen`), из
  них восемь — заголовок. Нынешняя строка это 103 символа; с префиксами вышло
  бы 117 при 120 доступных, и первое же добавленное поле молча срезало бы
  `bfar`. Расшифровка битов живёт в `cargo xtask panic`, потому что таблица на
  устройстве — это настоящий flash (в отличие от строк defmt, которые в образ
  не попадают), а на хосте она бесплатна. Резать строку где угодно можно ровно
  пока текст ASCII: `get_panic_message_utf8` на негодном UTF-8 не падает, но
  отдаёт валидный префикс — то есть разрубленный символ молча исчезает вместе
  с концом строки.
- **`bfar`/`mmfar` показываются только при `BFARVALID`/`MMARVALID`.** Регистры
  не сбрасываются между отказами: без бита там лежит адрес какого-то прошлого
  падения. Закреплено host-тестом
  `hides_the_address_when_the_core_says_it_is_stale`.
- **Vector catch отменяет запись, и это не дефект.** Поднял пробник ловушку на
  `HardFault` (так делает `probe-rs run`) — ядро встанет ДО обработчика.
  Ровно в этом случае за платой смотрят живьём; запись нужна тогда, когда не
  смотрит никто.
- **Флаг рекурсии в паникёре — `load`/`store`, а не `swap`.** Armv6-M не умеет
  атомарного обмена вовсе, и `swap` там не соберётся. Гонки нет: прерывания
  запрещены строкой выше. Деталь, как и снятие `USGFAULTENA`, перенесена из
  `panic-probe`.

