#![cfg_attr(not(test), no_std)]
// `async fn` в публичном трейте: rustc предупреждает (`async_fn_in_trait`), что
// у возвращаемого future нет границы `Send` и потребовать её вызывающий не
// сможет. Для однопоточного исполнителя embassy это ровно то, что нужно, а с
// `-D warnings` предупреждение стало бы ошибкой сборки. Так же поступает
// `embedded-hal-async` (его lib.rs, строка 10).
#![allow(async_fn_in_trait)]

//! Порты — границы `domain`: трейты, которые логика вызывает, не зная, что за
//! ними стоит. Реализации живут в `adapters` (переносимые: фейки для
//! host-тестов и драйверы generic по `embedded-hal`) и в `crates-cross/bsp`
//! (то, чему нужна периферия конкретного чипа), а сама логика ни о тех, ни о
//! других не знает и потому тестируется на хосте целиком.
//!
//! Правило, по которому решается, что сюда класть: если для проверки логики
//! нужно железо — значит между логикой и железом не хватает порта. Обратное
//! правило не менее важно: порт заводится под то, чем пользуется логика, а не
//! под каждую железную деталь. Причина прошлой паники домену не нужна — это
//! диагностика старта, и у неё метод `Board::last_panic()`, а не трейт.
//!
//! Три порта ниже — не выдуманные примеры: за каждым стоит то, что `bsp`
//! умеет отдавать приложению. [`BootCounter`] реализован всегда, а
//! [`FirmwareUpdate`] и [`SettingsStorage`] — когда при генерации выбраны OTA
//! и раздел настроек; сами трейты объявлены безусловно, потому что Liquid в
//! этом крейте запрещён (он компилируется и в самом репозитории шаблона).
//! Проект, где обе возможности выключены, получает их без реализаций — как
//! заготовку под своё хранилище или свой канал обновления.
//!
//! [`TemperatureSensor`] — четвёртый, он показывает форму для датчиков вашего
//! устройства (образец реализации — `adapters::Lm75`).

/// Источник температуры.
///
/// Милликельвины-целые, а не `f32`: у большинства Cortex-M нет FPU, и каждая
/// операция с плавающей точкой превращается в вызов softfloat из compiler-rt —
/// незаметно в исходнике, заметно в размере образа и во времени отклика.
/// Датчики в любом случае отдают целое из АЦП, так что перевод в дробное — это
/// потеря, которую делают один раз на границе с человеком, а не внутри логики.
pub trait TemperatureSensor {
    /// Чем может закончиться неудачное чтение: таймаут шины, NACK, битая
    /// контрольная сумма. Ассоциированный тип, а не общий `Error` крейта, —
    /// у каждого адаптера свой набор отказов, и заранее он не известен.
    type Error;

    /// Одно измерение в миллиградусах Цельсия (25.5 °C -> 25_500).
    ///
    /// Асинхронный, потому что за портом стоит шина: в Embassy обмен по
    /// I2C/SPI идёт через DMA, и синхронная сигнатура заставила бы блокировать
    /// исполнитель на всю транзакцию.
    async fn read_millicelsius(&mut self) -> Result<i32, Self::Error>;
}

/// Счётчик запусков, переживающий сброс.
///
/// Порт, а не деталь `bsp`, потому что им пользуется логика: «три неудачных
/// старта подряд — откатиться на прежний образ» — правило домена, а не
/// свойство регистра.
pub trait BootCounter {
    /// Отмечает очередной запуск и возвращает его номер (первый — 1).
    fn bump(&mut self) -> u32;
}

/// Обновление прошивки: то, что нужно приложению, а не каналу доставки.
///
/// Применение обновления (`mark_updated`/`verify_and_mark_updated`) сюда
/// намеренно не входит: его сигнатура зависит от того, выбрана ли подпись при
/// генерации проекта, и живёт рядом с каналом доставки — в `bsp`.
pub trait FirmwareUpdate {
    /// Отказ флеша или состояние, в котором операция запрещена (например,
    /// запись в раздел обновления до того, как подтверждён текущий образ).
    type Error;

    /// Самый длинный образ, который устройство способно принять и запустить.
    ///
    /// Нужна до начала приёма: длину образа сообщает недоверенный канал, и
    /// сверить её стоит раньше, чем начинать стирать флеш и принимать пакеты.
    ///
    /// Это не обязательно размер раздела, куда образ пишется: у схемы с
    /// обменом разделов приёмник по построению больше активного, и образ,
    /// влезший в приёмник, может не поместиться туда, где ему предстоит
    /// исполняться. Порт обещает меньшую из величин.
    ///
    /// `&mut self` — потому что реализация может читать границы у железа, а не
    /// хранить их числом; `Result` — по той же причине.
    fn capacity(&mut self) -> Result<u32, Self::Error>;

    /// Готовит раздел обновления к приёму образа длиной `len` — стирает под
    /// него место.
    ///
    /// Стирается не весь раздел, а столько страниц, сколько нужно образу: на
    /// типичном «образ занимает половину раздела» это вдвое меньше времени
    /// (см. предупреждение ниже) и заметно меньше износ флеша. Длину знать
    /// обязательно; если ваш канал её не сообщает, передайте
    /// [`capacity`](Self::capacity) — подготовится место под самый большой
    /// образ, какой устройство вообще примет.
    ///
    /// Нулевая длина и длина сверх [`capacity`](Self::capacity) — ошибка, а не
    /// молчаливое «сколько влезет»: узнать о негодной длине нужно ДО стирания,
    /// потому что стирание уничтожает образ, в который устройство откатывается.
    ///
    /// Зовётся один раз перед первым [`write`](Self::write), а не при каждом
    /// куске: после подготовки запись любого куска в любом порядке кладёт байты
    /// ровно туда, куда просили.
    ///
    /// Без явной подготовки эту работу пришлось бы делать записи, а она видит
    /// только свой кусок и не знает, начало это передачи или её продолжение.
    /// Ровно на этом и ловится обычная ошибка: «стирать сектор, если он новый»
    /// затирает уже принятое, стоит передаче начаться заново или прийти второму
    /// куску того же сектора.
    ///
    /// # Это самый долгий вызов во всём обновлении
    ///
    /// Стирание занимает от сотен миллисекунд (страницы по паре килобайт) до
    /// нескольких секунд (сектор в четверть мегабайта), и всё это время
    /// исполнитель не работает: драйверы флеша стирают внутри критической
    /// секции. Отсюда два следствия:
    ///
    /// * узел графа, у которого `watchdog:` короче времени стирания, будет
    ///   сброшен сторожем прямо посреди подготовки — либо зовите `prepare` до
    ///   старта графа, либо давайте узлу заведомо больший запас;
    /// * задачи, которым нужен отклик (приём по шине, мигание индикатором), на
    ///   это время замирают — планируйте подготовку туда, где пауза допустима.
    fn prepare(&mut self, len: u32) -> Result<(), Self::Error>;

    /// Пишет кусок нового образа по смещению от начала раздела обновления.
    ///
    /// Требует, чтобы раздел был подготовлен [`prepare`](Self::prepare).
    /// Что будет иначе, зависит от чипа: большинство семейств отвечают ошибкой
    /// программирования, а F2/F4/F7 принимают запись **молча**, оставляя в
    /// ячейке побитовое И нового значения со старым. Полагаться ни на первое,
    /// ни на второе нельзя — стирайте явно.
    ///
    /// Смещение `u32`, как и у чтения ниже. У `embassy-boot` эти две функции
    /// разошлись (`usize` против `u32`), и на 32-битной цели разница не видна
    /// — зато видна у фейка, скомпилированного под хост, где `usize` вдвое
    /// шире: одно и то же смещение перестало бы выражаться обоими типами.
    fn write(&mut self, offset: u32, data: &[u8]) -> Result<(), Self::Error>;

    /// Читает записанное обратно — проверить принятое, не полагаясь на то, что
    /// запись прошла успешно.
    fn read(&mut self, offset: u32, buf: &mut [u8]) -> Result<(), Self::Error>;

    /// Подтверждает работоспособность текущего образа и отменяет откат.
    ///
    /// Без этого вызова обновление живёт ровно один запуск: следующий сброс
    /// вернёт предыдущий образ.
    fn mark_booted(&mut self) -> Result<(), Self::Error>;
}

/// Настройки, переживающие перезапуск и обновление прошивки.
///
/// Ключ числовой: строковый пришлось бы писать во флеш целиком при каждом
/// сохранении, а раздел под настройки — это две страницы стирания.
pub trait SettingsStorage {
    /// Отказ флеша или повреждение данных в разделе.
    type Error;

    /// Читает значение по ключу; `None` — ключ ещё не записан.
    ///
    /// Буфер отдаёт вызывающий, потому что результат ссылается прямо на него:
    /// значение не копируется.
    async fn read<'a>(
        &mut self,
        key: u32,
        scratch: &'a mut [u8],
    ) -> Result<Option<&'a [u8]>, Self::Error>;

    /// Записывает значение, логически затирая предыдущее с тем же ключом.
    async fn write(&mut self, key: u32, value: &[u8]) -> Result<(), Self::Error>;
}
