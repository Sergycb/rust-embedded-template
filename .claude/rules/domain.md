---
paths: crates-host/domain/**
---

- `no_std`, но тестируется на хосте (`cargo xtask test host`). Зависимости от
  `embassy-executor` и `embassy-stm32` здесь запрещены — всё, что их требует,
  живёт в `crates-cross`.
- Liquid в `ports` и `adapters` запрещён: оба крейта — члены корневого
  workspace и компилируются в самом репозитории шаблона. Трейты объявляются
  безусловно, условными бывают только реализации в `crates-cross/bsp`.
- Асинхронный код и стейтчарты — это логика, и место им здесь, а не в
  `cross`, даже если результат используется только оттуда.
