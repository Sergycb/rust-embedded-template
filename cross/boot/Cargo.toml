[package]
name = "boot"
version = "0.1.0"
edition = "2024"
license = "MIT"

[[bin]]
name = "boot"
harness = false

[features]
# Значение подставляется каскадом выбора чипа (chip-select.rhai), не
# задаётся вручную. См. cross/bsp/Cargo.toml (та же фича, тот же смысл, тот
# же осознанный отказ от форварда на "embassy-stm32/_dual-core") — здесь для
# main.rs bootloader'а вместо Board::init().
default    = [{% if dual_core == "true" %}"dual-core"{% endif %}]
dual-core  = []

[dependencies]
cortex-m = { workspace = true }
cortex-m-rt = { workspace = true }
# Boot намеренно не получает release-транспорт defmt (UART без пробника,
# см. cross/app/src/task_orchestration.rs): у него нет embassy-executor и
# Spawner'а под фоновую drain-задачу, а единственная диагностическая строка
# перед прыжком в прошивку не стоит того, чтобы тянуть ради неё в bootloader
# целый UART-стек. Здесь только RTT.
defmt-rtt = { workspace = true }
defmt = { workspace = true }
panic-probe = { workspace = true, features = ["print-defmt"] }
panic-persist = { workspace = true }
embassy-boot-stm32 = { workspace = true, features = ["defmt"] }
embassy-stm32 = { workspace = true, features = ["defmt"] }
embassy-sync = { workspace = true }
