[build]
target = "{{target}}"

[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = "probe-rs run --chip {{chip}}"
rustflags = [
	"-C",
	"target-cpu={{cpu}}",
	"-C",
	"linker=flip-link",
	"-C",
	"link-arg=-Tlink.x",
	# defmt всегда используется (см. CLAUDE.md, "Feature-flag конвенции") —
	# в отличие от прежней схемы с log-only release-профилем, безусловно
	# нужен и в build.rs подставлять его больше незачем.
	"-C",
	"link-arg=-Tdefmt.x",
	"-C",
	"link-arg=--nmagic",
]

[env]
DEFMT_LOG = "info"
